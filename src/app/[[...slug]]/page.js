import CCSTalksApp from "@/components/ccs-talks/CCSTalksApp";

/** Every public Talks UI share this client shell (`/forum`, `/profile/me`, `#…` redirects, …). */
export default function TalksSlugPage() {
  return <CCSTalksApp />;
}
